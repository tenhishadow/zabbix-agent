---

- hosts: localhost
  gather_facts: "true"
  vars:
    zabbix_agent_config:
      'Server=': "Server=mon.tenhi.online"
      'Hostname=': "Hostname=travisci"
      'LogFile=': "LogFile=/dev/null"
  roles:
  - "zabbix-agent"

...
